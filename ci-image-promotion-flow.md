# CI Image Build and Promotion Flow

This document describes how CI build root images are built, promoted, and consumed
by openstack-k8s-operators repos.

```mermaid
graph TD
    subgraph "openstack-k8s-operators-ci repo (GitHub)"
        A["images/build-root/Dockerfile.rhel-9.custom<br/><i>FROM registry.ci.openshift.org/openshift/release:<br/>rhel-9-release-golang-X.Y-openshift-Z.W</i>"]
    end

    subgraph "openshift/release repo"
        B["ci-operator/config/openstack-k8s-operators/<br/>openstack-k8s-operators-ci/<br/>...-main.yaml<br/><i>Defines image builds + promotion</i>"]
        C["ci-operator/jobs/openstack-k8s-operators/<br/>openstack-k8s-operators-ci/<br/>...-main-postsubmits.yaml<br/><i>Generated by: make update</i>"]
        D["ci-operator/config/openstack-k8s-operators/<br/>infra-operator/...-main.yaml<br/><i>build_root: from_repository: true</i>"]
        E["ci-operator/config/openstack-k8s-operators/<br/>openstack-operator/...-main.yaml<br/><i>build_root: from_repository: true</i>"]
    end

    subgraph "Prow CI (build08 cluster)"
        F["Postsubmit Job:<br/>branch-ci-openstack-k8s-operators-<br/>openstack-k8s-operators-ci-main-images<br/><i>--promote --target=[images]</i>"]
    end

    subgraph "CI Image Registry"
        G["openstack-k8s-operators/tools:<br/>ci-build-root-golang-X.Y-sdk-A.B.C"]
    end

    subgraph "Consumer Repos (GitHub)"
        H["infra-operator/.ci-operator.yaml<br/><i>name: tools<br/>namespace: openstack-k8s-operators<br/>tag: ci-build-root-golang-X.Y-sdk-A.B.C</i>"]
        I["openstack-operator/.ci-operator.yaml<br/><i>name: tools<br/>namespace: openstack-k8s-operators<br/>tag: ci-build-root-golang-X.Y-sdk-A.B.C</i>"]
    end

    subgraph "Consumer CI Jobs"
        J["PR/functional test pods<br/><i>pipeline:root -> pipeline:src</i>"]
    end

    A -->|"Dockerfile used by"| B
    B -->|"make update generates"| C
    C -->|"triggers on merge to main"| F
    F -->|"builds images from Dockerfile<br/>and promotes to imagestream"| G
    G -->|"pulled as build_root"| D
    G -->|"pulled as build_root"| E
    H -->|"references"| G
    I -->|"references"| G
    D -->|"configures"| J
    E -->|"configures"| J
    H -.->|"tells ci-operator<br/>which image to use"| D
    I -.->|"tells ci-operator<br/>which image to use"| E

    style A fill:#f9d,stroke:#333
    style G fill:#9f9,stroke:#333
    style F fill:#ff9,stroke:#333
    style J fill:#9df,stroke:#333
```

## Key Flow

1. **PR merged** to `openstack-k8s-operators-ci` main branch
2. **Postsubmit job** (`branch-ci-openstack-k8s-operators-openstack-k8s-operators-ci-main-images`) triggers on the `build08` cluster, builds images from `Dockerfile.rhel-9.custom`
3. **Promotion** pushes the built image to the `openstack-k8s-operators/tools` imagestream (e.g. tag `ci-build-root-golang-1.24-sdk-1.41.1`)
4. **Consumer repos** (infra-operator, openstack-operator, etc.) reference that tag in their `.ci-operator.yaml` via `build_root_image`
5. **ci-operator** reads `build_root: from_repository: true` in the openshift/release config, looks up `.ci-operator.yaml` in the consumer repo, and pulls the referenced image as the build root
6. **Test pods** (functional, precommit, etc.) run using that build root as the `src` base image

## Important Notes

- The Go version in the build root is determined by the `FROM` base image in `Dockerfile.rhel-9.custom`, not by the image tag name
- Until the postsubmit promotion job completes successfully, all consumer repos continue using the previous version of the image
- The postsubmit job status can be checked at: `https://prow.ci.openshift.org/?job=branch-ci-openstack-k8s-operators-openstack-k8s-operators-ci-main-images`
